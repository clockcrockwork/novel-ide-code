観点別レビューと `/security-review` がカバーしない、このプロジェクト固有の規約を確認する。

**要求との一致**
- [ ] 要求された内容のみを変更しているか（要求外変更・ついで修正の混入がないか）
- [ ] 既存仕様を勝手に変更していないか

**プロジェクト規約**（[CLAUDE.md](../../CLAUDE.md) / [INVARIANTS.md](../data-model/INVARIANTS.md) 参照）
- [ ] kind 判定はフラグベースか（`kindId` 直接比較は禁止 — `hasFlag()` 経由）
- [ ] IDB 書き込みに `await` が漏れていないか
- [ ] `useUIStore` はセレクターで購読しているか（全 Store 購読は再レンダリングを増大させる）
- [ ] `localStorage` を直接操作していないか（IDB 経由が正）
- [ ] UI テキストが日本語か
- [ ] プロトタイプ汚染対策：動的キーを持つオブジェクトが `Object.create(null)` で初期化されているか
- [ ] パス検証がセグメント単位か（`startsWith('.github')` 単独は禁止）
- [ ] [verification-gates.md](../ai/rules/verification-gates.md)「モバイル viewport / iOS キーボード」行のトリガー集合（正本。列挙はここに複製しない）に該当する変更で、iOS キーボード表示時に下部 UI が隠れないか（`100dvh` と `#root` 縮小の乖離）を確認したか。確認手段は同行のゲート `npm run test:e2e -- e2e/mobile/`（pretest のポート掃除を経由。実行不能環境では理由を明記して見送る — 同ファイル anti-skip 経路）（PR #167 の回帰クラス）

**想定ケース（リスクモデリング）**（[risk-modeling.md](risk-modeling.md)）
- [ ] 実装計画または作業メモに想定ケース表があり、「今回対応しないもの」に理由（⏭️ は前提＝依存する外部状態を名指し。[risk-modeling.md](risk-modeling.md)）が付いているか
- [ ] 不正系・異常系テストの要否が検討されているか（見送る場合は理由が残っているか）
- [ ] 未作成の場合はこの段階で [risk-modeling.md](risk-modeling.md) の手順に従って洗い出したか

**既存実装調査**（[codebase-recon.md](codebase-recon.md)）
- [ ] 実装計画または作業メモに既存実装調査表があり、実行した検索クエリが記録されているか
- [ ] 新規に作った util / component / 関数に「探して無かった」検索記録と、既存候補を使わない理由が付いているか
- [ ] 実装中に増えたファイル・関数・依存が調査表に追記されているか（この段階での事後調査は差し戻し扱い。理由を残して調査してから進む）

**テスト**
- [ ] 貼り付け・クリップボード・キーボード入力の挙動を変更した場合、`e2e/editor/` または `e2e/security/` に Playwright E2E テストを追加したか（入力経路は単体テストのみでは回帰を検知しにくい）

**AI 固有項目**
- [ ] 実在しない helper / method を前提にしていないか
- [ ] `eslint-disable` で lint エラーを抑制していないか（根本修正が先）
- [ ] 振る舞いを変えた際に既存テストが「たまたま pass」していないか確認したか

発見した問題はすべてステップ 6 で修正する。