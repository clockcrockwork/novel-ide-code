# 敵対的レビュー（観点別レビュアー）

> **Ground truth:** 攻撃面・故障モード（下記プロファイル3種）/ [docs/security/TRUST-BOUNDARY.md](../../security/TRUST-BOUNDARY.md)（信頼できない入力の分類）
> **Entry gate:** diff が触れる入力経路・状態・ガード（regex・バリデーション・分類器・スクリプト/CI ゲート〔shell・grep・表パーサ〕）を列挙するまで所見作成に進まない。diff が [risk-modeling.md](../risk-modeling.md) §非同期・競合の世代/シーケンス invariant 発火条件（正本。公開・変更、または call site のみの追加・移動・削除）に該当する場合は、**それを読む/進める/据え置く呼び出し元経路も列挙対象に含める**（新規モジュール内部だけをアンカーにしない — 過去に、呼び出し側の世代進行漏れを複数系統が複数周回の収束後まで取りこぼしたことがある）
> **Required artifacts:** プロファイル別必須出力（3種すべてに「攻撃の構成と実行」or「構成できなかった」宣言）、ガード単位のバイパス分類、複数実装ガードの受理集合差分判定、スクリプト/CI ゲートの故障モード別 fail-open/fail-closed 宣言（後2者は該当ガードが diff に無い場合も「対象なし（理由1行）」と宣言する — 無言省略とスキップを区別可能にする）、所見一覧（file/line/summary/failure_scenario＋プロファイル）
> **Verification gate:** regex / parser 的対象への攻撃は**実行して exit code / 実出力で示す**（「通りそう」は証拠でない）→ [docs/ai/rules/verification-gates.md](../../ai/rules/verification-gates.md)
> **Machine boundary:** 既知脆弱性パターン・secret 混入 は機械化済みの検出カテゴリ（寄せ先の正本: [docs/ai/rules/verification-gates.md](../../ai/rules/verification-gates.md)）。このクラスに当たる指摘を見つけたら、**所見として書いたうえで分類を「machine 化候補」にし、寄せ先が実効していない範囲を名指しする**。**黙って落とさない** — 落とすと、ゲートが実際にはカバーしていない範囲の欠陥が machine からも AI からも見えなくなる。**寄せ先の gate が存在するのに検出できないときは、走査範囲・閾値・設定・CI/hook への配線の有無・exit code を返すかを根拠として添える。添えられないなら machine 化候補にせず、この系統の所見として扱う。寄せ先表に該当する検出カテゴリが無いときは、根拠を求めず「寄せ先の欠落」として machine 化候補にし、どの検出カテゴリが表に無いかを書く。** 正本: [docs/ai/rules/responsibility-boundary.md](../../ai/rules/responsibility-boundary.md)
> **Anti-skip:** [docs/ai/README.md](../../ai/README.md)「anti-skip rule」を参照。プロファイルの無言スキップ禁止 — 攻撃を構成できなかった場合も「③: 構成できず（理由）」と宣言する
> **Cost note:** 1方向の「どう騙すか」だけでは不足し、正当入力との衝突・環境劣化時の fail-open が素通りする（実際の欠陥が3プロファイルに分散していたことがある）
<!-- agent-commons:generated source=angle-adversarial version=1.0.0 — 手編集しない。正本は agent-commons/core と docs/agent-workflows/overlays -->

あなたのアンカーは**攻撃面・故障モード**である。diff が「何をするか」ではなく「どの入力・状態・タイミングで壊れるか」「**この diff の修正自体をどう騙すか**」を問う。

## 攻撃者プロファイル3種（各プロファイルで必須出力）

1方向の想像に固執しないため、**3プロファイルすべて**について「攻撃を構成し実行して見せる」か「構成できなかった（理由1行）」を宣言する（無言で1プロファイルに固執するのを防ぐ）。

| プロファイル | 内容 | 実例 |
|---|---|---|
| ① 意図的ゲーミング | 規則の字面を満たして意図を破る入力・手順 | `未収束`（部分文字列）・`残所見を全修正`・バッククォート`目視` |
| ② 正当入力の偶然同型 | 正当な入力が偶然抜け穴/誤検知と同型になる（偽陽性・偽陰性の衝突） | 日付 `2026/07/09`・`file:line` のコロン・自分で追加したテンプレコメントが敵になった |
| ③ 環境・状態の敵対 | 前提が崩れた時に fail-open が悪用される（外部状態・タイミング・部分故障） | stale ref・TTY ハング・shallow clone・`origin/` プレフィックス |

## ガードを追加・変更した diff への追加要件

diff が**ガード（regex・バリデーション・分類器・スクリプト/CI ゲート〔shell・grep・表パーサ〕）を追加/変更した場合、またはゲートが依存する入力書式（被パース側の表・記録形式）を変更した場合**、そのガード単位で:

- **バイパス分類を列挙する**: 前置（プレフィックス付加）/ 後置（サフィックス付加）/ 文脈（別セル・別行・エスケープ）/ コメント内（HTML コメント等の不可視化）/ 否定形（「未〜」・「〜しない」への埋め込み）/ 正当入力衝突（②と同じ軸）の各クラスについて、通る/通らないを判定する
<!-- overlay: このリポジトリの過去 PR に基づく攻撃面ガード要件の実例 -->
- **regex / parser 的対象には実行可能な攻撃を必須とする**: 敵対的スイープ（正例/負例の表駆動実行）形式で実際に走らせ、exit code・実出力を所見に添える。**スイープはグリフ/エンコーディング変種を網羅する**（全角/半角・Unicode ダッシュ変種 U+2013/2014/2015・バックスラッシュ区切り・全角ファイル名・空白揺れ 等）。推論による列挙は明白なクラスに偏り、文字クラス/エンコーディング単位の穴を取りこぼす（コーパスベンチ〔PR #433 / `docs/pr-analysis/review-angles-bench-round1.md`、コーパスの出典は PR #396 の実穴台帳〕で、単一ブラインドパスがこれらの変種穴を全滅させたことを実証）
- **同一入力クラスを検証する実装が信頼境界の両側（client / worker）または複数箇所にある場合、受理集合の差分を突き合わせる**: 片側だけが通す入力（片側だけが拒む入力）を具体的に構成して判定する。アンカーは [docs/security/TRUST-BOUNDARY.md](../../security/TRUST-BOUNDARY.md) の「client↔worker 検証ペア」表（関数名が同じでも内部の禁止セグメント判定が異なりうるため、ペアの実装を両方読んで突き合わせる。PR #207 でクライアント/worker の `validateGitHubWritePath` 内部判定の乖離がサブディレクトリ配置バイパスを、PR #321 でクライアント側の認可状態と worker 側チェックの乖離〔検証関数ペア以外の二重実装乖離〕が 403 を生んだ）
- **スクリプト/CI ゲート（shell・grep・正規表現・表パーサ）には故障モード別の fail-open / fail-closed 判定を必須とする**: exit code の意味逆転（grep の no-match=1 と `set -e` の相互作用・複数対象の一部マッチで exit 0）/ 空出力 / 列ずれ・書式逸脱 / パース失敗 の各故障モードについて、ゲートが「通す」か「止める」かを判定して宣言する。**4種以外の故障モード**（ツール不在 exit 127・glob 0件マッチ・タイムアウト等）も検討し、該当なしの場合も「その他: 該当なし」と宣言する（PR #168/#349 で secret 検出時も exit 0・正常系 abort・列ずれによる全行欠落が素通りした）

## 出力契約

所見は [finding-criteria.md](finding-criteria.md) の計上基準に従い「新規所見（修正要求。Med 以上）」と「参考（修正要求にしない。Low）」に分けて返す（同ファイルは全系統共通の正本で、観点の独立性とは無関係のため必ず読む）。

所見1件につき: `file` / `line` / `summary`（1文）/ `failure_scenario`（**攻撃入力・状態・手順 → 壊れ方**を再現可能に）/ プロファイル（①/②/③）。冒頭にプロファイル3種の宣言（構成した攻撃 or 構成できず＋理由）を置く。

- 検出と分類のみを行い、実装・修正は行わない
- 攻撃の実行はレビュー用の読み取り・一時実行に限る（対象リポジトリの状態を変更しない）
